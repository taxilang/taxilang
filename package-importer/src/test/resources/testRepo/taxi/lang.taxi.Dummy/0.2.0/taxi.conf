name: taxi/Demo
version: 0.2.0
dependencies: {
   "https://${gpat}@github.com/taxi/Another": 0.3.0
}
publishToRepository: {
   url: "http://localhost:8081/"
   settings: {
      repositoryName: taxi
   }
}
