package lang.taxi.generators.soap

/**
 * Extracts the XSD schema from a WSDL document,
 * such that it can be parsed by XSOM.
 *
 * The XML world in Java is a mess - quick brain dump...
 *
 *  - XSOM (by glassfish) is the library for parsing XSD documents.
 *  - WSDLs con be parsed by Apache CXF, which supports
 */
class SchemaExtractor {
}
