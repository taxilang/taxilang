package org.taxilang.openapi

import io.kotest.core.spec.style.DescribeSpec

class OpenApiGeneratorPluginTest : DescribeSpec({

   describe("open api plugin") {
      it("should generate openapi specs when the plugin is configured") {

      }
   }
})
